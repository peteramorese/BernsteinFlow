[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bernstein-flow"
version = "0.1.0"
description = "Bernstein Flow project"

[tool.setuptools.packages.find]
where = ["src"]